#include <stdc++.h>
using namespace std;

int removeElement(vector<int> &nums, int val)
{
    // int k;
    // vector<int> temp;

    // for (int i = 0; i < nums.size(); i++)
    // {
    //     if (nums.at(i) != val)
    //     {
    //         temp.push_back(nums.at(i));
    //     }
    // }
    // nums = temp;
    // k = nums.size();
    // return k; 
}